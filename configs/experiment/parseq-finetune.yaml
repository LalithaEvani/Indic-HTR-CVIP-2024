# @package _global_
# Fine-tuning preset: higher permutation count and dropout, lower learning
# rate. Requires ckpt_path=<path to a baseline checkpoint> to resume from.
defaults:
  - override /model: parseq

model:
  perm_num: 14
  dropout: 0.4
  lr: 7e-6
